---
description: Gather external knowledge into §R so build grounds in facts. Every finding cites a source.
argument-hint: "[topic | \"best lib for X\"]"
---
Use the `cavekit-research` skill workflow for this request.

Arguments: $ARGUMENTS

Scope to concrete questions, gather from primary sources, distill each answer to one caveman §R row + its source, and hand the rows to `cavekit-spec`. Flag any unverified finding `?` — never write a guess as fact. Do not write `SPEC.md` directly; defer all writes to `cavekit-spec`. Do not commit unless explicitly requested.
